=ARRAYFORMULA(
  IF(
    ROW(AT:AT)=1, "Approver Name", 
    IFS(
      REGEXMATCH(H:H, "HOO or RMS"), "Charles Oliver",
      REGEXMATCH(H:H, "MP or PO"), "Charles Oliver",
      REGEXMATCH(H:H, "already approved"), "Charles Oliver"
    )
  )
)