=ARRAYFORMULA(
  IF(
    ROW(BH:BH)=1, "Approver Email", 
    
    IFS(
      REGEXMATCH(BG:BG, "Accounting"), "charles@inselfstorage.com",
      REGEXMATCH(BG:BG, "Charles"), "charles@inselfstorage.com",
      REGEXMATCH(BG:BG, "Justin"), "charles@inselfstorage.com"
    )
  )
)