=ARRAYFORMULA(
  IF(
    ROW(AX:AX)=1, "Links",

    IF(
      (ISBLANK(X:X)*ISBLANK(X:X)), "N/A",

      IF(
        ISBLANK(X:X), "", "<a href=" & X:X & ">Work Link</a>"
      ) &

      IF(
       (NOT(ISBLANK(X:X))*NOT(ISBLANK(Y:Y))), " - ", ""
      ) &

      IF( 
        ISBLANK(Y:Y), "", "<a href=" & Y:Y & ">Work List</a>"
      )
    )
  )
)